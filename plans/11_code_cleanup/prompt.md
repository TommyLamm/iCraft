# 單份計劃執行提示

將 `{{PLAN_FILE}}` 替換成索引中的一份文件。這是實作提示，並非本次已開始實作。

```text
請完整執行 iCraft 的以下計劃：
{{PLAN_FILE}}

先讀 ARCHITECTURE.md、plans/11_code_cleanup/README.md 與該計劃。依 source 確認前置；.codegraph 存在時先用 codegraph explore，結果不足再搜尋。不要重建索引。

先檢查工作樹，保留所有不屬於本任務的修改。本次只完成所選工作包及其必要caller、tests、imports、文件更新，不順手執行其他工作包。

積極刪除死實作、重複狀態和無用途API，不加相容殼，不靠allow隱藏問題。test-only原型可刪；有效產品測試改走正式入口。依具體資料流區分無producer、純轉接與活躍重構。

前置已具備就直接完成；若只是小型必要調整，連同本包處理並記錄。若缺少另一整包才能進行，指出具體source缺口與對應前置，不建立旁路或宣稱完成。

按計劃做窄驗證；新增測試只覆蓋明列的行為缺口，不為刪import等低影響修改增加測試。既有基線問題、改動導致的失敗、尚未量測的推論分開記錄。

架構或資料契約改變時，同包更新ARCHITECTURE.md。完成後在該計劃「實作紀錄」填改動、實際命令與結果、淨刪碼／保留原因；更新README狀態。沒有真正驗證的項目不可標已完成，也不捏造效能收益。
```
